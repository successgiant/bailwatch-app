// Colors mapped 1:1 from the dashboard CSS dark theme variables
export const Colors = {
  // -- Backgrounds --
  bg: "#030712",           // --background
  bgCard: "#040914",       // --card
  bgPanel: "#050b18",      // --sidebar
  bgInput: "#1a2942",      // --input
  secondary: "#0f1b35",    // --secondary
  accent: "#061028",       // --accent
  mutedBg: "#02050e",      // --muted (background variant)

  // -- Brand blue --
  blue: "#2563eb",         // --primary / --chart-1
  blueBright: "#3b82f6",   // --ring / --chart-2
  blueLight: "#60a5fa",    // --chart-3
  blueSky: "#38bdf8",      // --chart-4

  // -- Dashboard transparent-blue UI (from Activity Overview HTML) --
  blueSubtle: "rgba(59,130,246,0.10)",   // bg-blue-500/10
  blueBorder: "rgba(59,130,246,0.28)",   // border-blue-500/30
  blueIconBg: "rgba(59,130,246,0.10)",   // icon container bg
  blueIconBorder: "rgba(96,165,250,0.30)", // icon container border

  // -- Status --
  gold: "#f59e0b",
  green: "#22c55e",
  emerald: "#10b981",
  red: "#ef4444",          // --destructive
  orange: "#f97316",
  purple: "#c084fc",       // --chart-5

  // -- Text --
  text: "#ffffff",          // --foreground / --card-foreground
  muted: "#94a3b8",         // --muted-foreground / --sidebar-foreground
  mutedDim: "#64748b",      // dimmer secondary text

  // -- Borders --
  border: "#111c35",        // --border / --sidebar-border
  borderFaint: "#0f1b35",   // softer separator (= --secondary)

  // -- UI chrome --
  rowDivider: "rgba(255,255,255,0.04)",   // subtle table row separator
  dragHandle: "rgba(148,163,184,0.18)",   // modal bottom-sheet drag pill
}

export const FontSize = {
  xs: 11,
  sm: 13,
  md: 15,
  lg: 17,
  xl: 20,
  xxl: 24,
  h2: 32,
  h1: 40,
}

export const Radius = {
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
}

export const Font = {
  regular:   "Inter_400Regular",
  medium:    "Inter_500Medium",
  semibold:  "Inter_600SemiBold",
  bold:      "Inter_700Bold",
  extrabold: "Inter_800ExtraBold",
}

export const Spacing = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 10,
  xxl: 28,
}
